services:
  postgres:
    image: postgres:16
    container_name: project-manager-db
    environment:
      POSTGRES_DB: project-manager
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"